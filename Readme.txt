1 - Initialize a Node.js server to receive [GET] data and return it.

2 - Open the command prompt, obtain the IP address using 'Ipconfig', retrieve the IPv4 address, and then access the 'config.js' file to change the IP address (Only with the correct IP address can you access it).
